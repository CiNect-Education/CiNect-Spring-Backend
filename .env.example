# ============================================================
# CiNect Spring Backend - Environment Variables
# ============================================================

# --- Database (Supabase PostgreSQL) ---
# Lấy từ Supabase Dashboard > Project Settings > Database
# Dùng Connection String > JDBC (port 5432)
# Format: jdbc:postgresql://aws-0-[region].pooler.supabase.com:5432/postgres?user=postgres.[project-ref]&password=[your-password]
DB_URL=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.your-project-ref&password=your-password
DB_USERNAME=postgres.your-project-ref
DB_PASSWORD=your-supabase-db-password

# --- Hoặc dùng Local PostgreSQL ---
# DB_URL=jdbc:postgresql://localhost:5432/cinect_spring
# DB_USERNAME=postgres
# DB_PASSWORD=postgres

# --- JWT ---
JWT_SECRET=your-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
JWT_ACCESS_EXP=900000
JWT_REFRESH_EXP=604800000

# --- CORS ---
CORS_ORIGINS=http://localhost:3000

# --- Server ---
PORT=8080

# --- App Config ---
HOLD_TTL=10
PAYMENT_TIMEOUT=2
POINTS_PER_BOOKING=10
MAINTENANCE_MODE=false

# --- OAuth (Social Login) ---
# Google: https://console.cloud.google.com > APIs & Services > Credentials > OAuth 2.0
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:8081/api/v1/auth/google/callback

# Facebook: https://developers.facebook.com > My Apps > Facebook Login
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
FACEBOOK_CALLBACK_URL=http://localhost:8081/api/v1/auth/facebook/callback

# GitHub: https://github.com/settings/developers > OAuth Apps
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:8081/api/v1/auth/github/callback

# Frontend URL (for OAuth redirect after login)
FRONTEND_URL=http://localhost:3000
